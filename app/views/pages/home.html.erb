<% if signed_in? %> <!-- add microposts creation form to homepage -->
	<%= render 'pages/home_signed_in' %> <!-- the path relative to views dir should be used (the pages/ ) -->
<% else %>
	<%= render 'pages/home_not_signed_in' %>
<% end %>